name: tandachain
description: TandaChain — prototipo estático Flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^3.3.4
  confetti: ^0.7.0
  fl_chart: ^0.69.0
  go_router: ^14.6.2
  google_fonts: ^6.2.1
  intl: ^0.19.0
  stellar_flutter_sdk: ^1.8.0
  shared_preferences: ^2.2.2
  convert: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
